{
  "name": "pandolab",
  "version": "0.30.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build:country-schema": "node tools/normalize-canonical-country-schema.mjs",
    "check:country-schema": "node tools/normalize-canonical-country-schema.mjs --check",
    "build:mesh": "python tools/validate-country-geometry.py --canonical-only && node tools/build-world-mesh.mjs",
    "build:preview": "python tools/validate-country-geometry.py --canonical-only && node tools/build-world-preview.mjs && python tools/validate-country-geometry.py",
    "build:map-assets": "pnpm build:country-schema && pnpm build:mesh && pnpm build:preview",
    "build:historical-library": "python tools/build-historical-library.py",
    "check:historical-library": "python tools/build-historical-library.py --check && node scripts/check-historical-library.mjs",
    "generate:build-meta": "node scripts/generate-build-metadata.mjs",
    "check:version": "node scripts/check-version.mjs",
    "check:preview": "python tools/validate-country-geometry.py --canonical-only && node tools/build-world-preview.mjs --check && python tools/validate-country-geometry.py",
    "lint": "eslint assets/js tests/browser tests/unit scripts playwright.config.js eslint.config.js",
    "check:js": "node scripts/check-js.mjs",
    "check:ui-spacing": "node scripts/check-ui-spacing.mjs",
    "check:ui-components": "node scripts/check-ui-components.mjs",
    "check:architecture": "node scripts/check-runtime-boundaries.mjs",
    "test:python": "python -m unittest discover -s tests -p \"test_*.py\"",
    "test:unit": "node --test \"tests/unit/*.test.mjs\"",
    "test:browser": "playwright test",
    "test": "eslint assets/js tests/browser tests/unit scripts tools playwright.config.js eslint.config.js && node scripts/check-js.mjs && node scripts/check-version.mjs && node scripts/check-runtime-boundaries.mjs && pnpm check:country-schema && pnpm check:historical-library && python -m unittest discover -s tests -p \"test_*.py\" && node scripts/check-ui-spacing.mjs && node scripts/check-ui-components.mjs && node --test \"tests/unit/*.test.mjs\" && playwright test"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "eslint": "10.9.1",
    "topojson-client": "3.1.0",
    "topojson-server": "3.0.1",
    "topojson-simplify": "3.0.3"
  }
}
